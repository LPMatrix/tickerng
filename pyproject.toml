# PEP 621 project for Vercel Python (`uv lock`). Agent deps: Langfuse prompt overrides + stdlib elsewhere.
[project]
name = "tickerng-research-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langfuse>=4.5.1",
]
